# Inspeção do inventário histórico

O mapeamento nominal encontrou apenas um candidato entre 33 fontes ativas do staging. Antes de baixar conteúdo histórico, a próxima inspeção deve resumir os formatos, diretórios e tamanhos predominantes em `processed`, `warehouse` e `exports`, além de identificar o único par nominal retido.

Essa inspeção usa apenas os relatórios locais já produzidos e não altera o Google Drive.
